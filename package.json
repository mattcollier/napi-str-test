{
  "name": "napi-str-test",
  "version": "0.0.1",
  "main": "test.js",
  "scripts": {
    "start": "node test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "benchmark": "^2.1.4"
  },
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-digitalbazaar": "^2.6.1"
  }
}
